<!--
Fill in the sections below. Delete any that genuinely don't apply, but
default to filling them in rather than deleting them.
-->

## Summary

<!-- What changed and why, in a few bullet points. -->

-

## Which plugin(s)

<!-- xo-server-nanokvm, xo-server-host-power-manager, or both -->

## Checklist

- [ ] Each changed `package.json` is still valid JSON
- [ ] `node --check` passes on every changed `.js` file
- [ ] Bumped the affected plugin's `package.json` version, if this is a
      user-facing change
- [ ] Added a [CHANGELOG.md](../CHANGELOG.md) entry under `[Unreleased]`,
      naming the affected plugin
- [ ] Updated the affected plugin's own `README.md` if this changes install
      or configuration behavior

## Test plan

<!-- How was this actually verified? What was run, against what. -->
